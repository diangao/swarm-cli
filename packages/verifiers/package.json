{
  "name": "@swarm/verifiers",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./dist/index.js"
    }
  },
  "dependencies": {
    "@swarm/protocol": "workspace:*",
    "@swarm/security": "workspace:*",
    "@swarm/testkit": "workspace:*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm build && tsc -p tsconfig.test.json && node --test build/test/*.test.js"
  }
}
