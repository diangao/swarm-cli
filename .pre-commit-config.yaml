repos:
  - repo: local
    hooks:
      - id: publication-gate
        name: publication gate
        entry: python3 scripts/publication_gate.py --staged
        language: system
        pass_filenames: false
        stages: [pre-commit]
